[package]
name = "ai-news-generation"
version = "0.1.0"
edition = "2021"

[dependencies]
ai-news-core = { path = "../core" }
ollama-rs = { version = "0.2", features = ["stream"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
slug = "0.1"
thiserror = "2"
tracing = "0.1"
